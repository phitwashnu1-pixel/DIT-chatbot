# คู่มือสรุปการพัฒนาระบบ DIT ChatBot (Development & System Manual)

เอกสารฉบับนี้จัดทำขึ้นเพื่อสรุปภาพรวมการพัฒนาระบบ โครงสร้างทางเทคโนโลยี และคู่มือสำหรับการตอบคำถาม (Q&A) ในกรณีที่มีการนำเสนอผลงาน หรือส่งมอบระบบแชทบอทของแผนกธุรกิจดิจิทัลและเทคโนโลยีสารสนเทศ วิทยาลัยอาชีวศึกษาสุราษฎร์ธานี

---

## 1. สรุปภาพรวมระบบ (System Overview)
- **ชื่อโปรเจกต์:** DIT ChatBot (แชทบอทผู้ช่วยตารางเรียน-ตารางสอน)
- **วัตถุประสงค์:** เพื่อให้บริการข้อมูลตารางเรียน ตารางสอน และตารางการใช้ห้องเรียน ผ่านรูปแบบการสนทนา (Chat) ที่ขับเคลื่อนด้วยเทคโนโลยี AI ทำให้ผู้ใช้ (นักเรียน/ครู) สามารถเข้าถึงข้อมูลได้อย่างสะดวกรวดเร็ว
- **เทคโนโลยีหลักที่ใช้ (Tech Stack):**
  - **Frontend:** Next.js 15 (React), Tailwind CSS (พัฒนา UI แบบ Responsive)
  - **AI Engine:** Google Gemini API (`gemini-3.5-flash`) ผ่านแพ็กเกจ Vercel AI SDK
  - **Data Source:** แหล่งข้อมูลแบบ Local JSON Files (จัดเก็บและจัดการง่าย)
  - **Deployment:** Vercel (รองรับ CI/CD และ Serverless API)

---

## 2. โครงสร้างและการทำงานของระบบ (Architecture)

การทำงานของระบบถูกแบ่งออกเป็น 3 ส่วนหลัก (3-Tier Architecture) ดังนี้:

### 2.1. Data Layer (ชั้นการจัดการข้อมูล)
- ข้อมูลดิบถูกแยกเก็บในโฟลเดอร์ `Data/Classroom/`, `Data/Teacher/`, และ `Data/Timetable/` ในรูปแบบไฟล์ `.json`
- มีการใช้สคริปต์ Node.js (`scripts/build-data.mjs`) ทำงานแบบอัตโนมัติก่อนกระบวนการ Build เพื่อรวบรวม (Compile) ไฟล์ JSON นับร้อยไฟล์ ให้กลายเป็นไฟล์เดียวคือ `src/data.json` เพื่อให้ระบบอ่านข้อมูลได้รวดเร็วที่สุด

### 2.2. API & AI Layer (ชั้นประมวลผลตรรกะและ AI)
- ใช้ Serverless Function ของ Next.js (`src/app/api/chat/route.ts`) เป็นตัวกลางในการสื่อสาร
- **ระบบ RAG (Retrieval-Augmented Generation):** ก่อนส่งคำถามให้ AI ระบบจะนำคำถามของผู้ใช้ไปค้นหา Keyword (เช่น ชื่อครู, เลขห้อง, รหัสวิชา) ใน `src/data.json` เพื่อดึงเฉพาะข้อมูลตารางที่เกี่ยวข้อง (Relevant Data) แนบไปให้ AI ทำให้ AI ตอบคำถามได้รวดเร็วขึ้นและไม่เปลือง Context Window
- **ระบบ Real-time Date/Time:** ระบบจะคำนวณวันและเวลาปัจจุบัน (Timezone: Asia/Bangkok) แนบเข้าไปใน System Prompt เสมอ เพื่อให้ AI รู้ตัวว่า "วันนี้/เวลานี้" คือเมื่อไหร่
- **ระบบ API Key Rotation:** มีการสุ่มใช้งาน Google Gemini API Keys หลายตัวสลับกัน เพื่อป้องกันการติดข้อจำกัด Rate Limit เมื่อมีผู้ใช้งานพร้อมกันจำนวนมาก

### 2.3. Frontend Layer (ชั้นการแสดงผลและติดต่อผู้ใช้)
- หน้าเว็บหลัก `src/app/page.tsx` ถูกออกแบบให้สวยงาม ทันสมัย (Modern UI/Glassmorphism)
- รองรับการใช้งานทุกหน้าจอ (Responsive Design) โดยเฉพาะบนสมาร์ทโฟน
- แสดงผลข้อมูลตารางเป็น Markdown Table แบบอัตโนมัติ พร้อมรองรับการเลื่อนซ้าย-ขวา (Horizontal Scroll) บนมือถือเพื่อไม่ให้หน้าจอเสียทรง

---

## 3. คู่มือสำหรับตอบคำถามในการพัฒนาระบบ (Q&A for Developers & Presentation)

รวบรวมคำถามที่มักจะถูกถามบ่อยในการนำเสนอระบบ และแนวทางการตอบคำถามอย่างมืออาชีพ:

**Q1: ทำไมถึงเลือกใช้ Next.js แทนที่จะเขียนด้วย HTML/JavaScript ธรรมดา?**
**แนวทางการตอบ:** "เนื่องจาก Next.js มีความสามารถในการทำ Full-Stack ได้ในโปรเจกต์เดียวครับ เราสามารถสร้างหน้าเว็บ (UI) ไปพร้อมๆ กับการสร้าง API (Backend) สำหรับติดต่อกับ AI ได้เลย ทำให้ระบบมีความเสถียร จัดการโค้ดได้ง่าย และทำงานร่วมกับ Server ของ Vercel ได้อย่างมีประสิทธิภาพสูงสุดแบบ Serverless ครับ"

**Q2: แชทบอทเอาข้อมูลตารางสอนมาจากไหน? มีโอกาสที่ AI จะมั่วข้อมูล (Hallucination) หรือไม่?**
**แนวทางการตอบ:** "ข้อมูลทั้งหมดดึงมาจากฐานข้อมูลตารางเรียน (ไฟล์ JSON) ของแผนกโดยตรงครับ และเราได้เขียน System Prompt บังคับ AI ไว้อย่างเคร่งครัดว่า **'ห้ามคิดข้อมูลขึ้นมาเองเด็ดขาด'** AI จะมีหน้าที่แค่นำข้อมูลดิบที่เราป้อนให้ ไปเรียบเรียงตอบกลับเป็นภาษามนุษย์และสร้างตารางที่สวยงามเท่านั้น ทำให้ข้อมูลมีความถูกต้องและแม่นยำสูงครับ"

**Q3: ถ้าระบบมีผู้ใช้งานพร้อมกันจำนวนมาก ระบบจะล่ม หรือติด Limit ของ AI ฟรีหรือไม่?**
**แนวทางการตอบ:** "เราได้ป้องกันปัญหานี้ด้วยเทคนิค **API Key Rotation** ครับ โดยในระบบเราฝัง API Key ไว้หลายชุด เมื่อมีคนถามคำถาม ระบบจะทำการ 'สุ่ม' สลับ Key ไปเรื่อยๆ เพื่อกระจายการประมวลผล ทำให้โอกาสที่จะติด Rate Limit ของ AI ลดลงอย่างมาก รองรับผู้ใช้งานพร้อมกันได้สบายครับ"

**Q4: แชทบอทรู้ได้อย่างไรว่า "วันนี้" คือวันอะไร หากนักเรียนถามว่า "วันนี้ครู...มีสอนไหม"?**
**แนวทางการตอบ:** "ในเบื้องหลังการทำงาน ทุกครั้งที่นักเรียนพิมพ์คำถาม ระบบ Backend จะทำการดึงข้อมูล 'วันและเวลาปัจจุบัน' อ้างอิงตามเขตเวลาประเทศไทย (Asia/Bangkok) และแอบแนบไปพร้อมกับคำถามเสมอครับ ทำให้ AI รับรู้สถานการณ์แบบ Real-time ตลอดเวลา"

**Q5: หากมีการปรับเปลี่ยนตารางสอนในภาคเรียนถัดไป จะต้องอัปเดตระบบอย่างไร?**
**แนวทางการตอบ:** "การบำรุงรักษา (Maintenance) ทำได้ง่ายมากครับ ผู้ดูแลระบบเพียงแค่นำไฟล์ JSON ตารางสอนชุดใหม่ ไปวางทับไฟล์เดิมในโฟลเดอร์ Data จากนั้นสั่ง Deploy ระบบใหม่เพียง 1 คำสั่ง (`npx vercel --prod --yes`) ระบบจะทำการรวบรวมข้อมูลใหม่ทั้งหมดและอัปเดตขึ้นเว็บไซต์ให้ทันทีครับ"

**Q6: หากในอนาคตต้องการเพิ่มฟังก์ชันใหม่ๆ เช่น การแนบไฟล์รูปภาพ ทำได้หรือไม่?**
**แนวทางการตอบ:** "สามารถต่อยอดได้แน่นอนครับ โครงสร้างระบบที่เราวางไว้ใช้ Vercel AI SDK ซึ่งรองรับฟีเจอร์ Multi-modal (ส่งภาพ/เสียง) ของ Gemini อยู่แล้ว ในอนาคตหากต้องการให้บอทอ่านรูปตารางสอน ก็สามารถเขียนโค้ดเพิ่มในโครงสร้างเดิมได้เลยโดยไม่ต้องรื้อระบบทำใหม่ครับ"

---

## 4. มาตรการด้านความปลอดภัยและการรับมือข้อจำกัดระบบ (Security & Rate Limit Handling)

เพื่อความเสถียรและความปลอดภัยสูงสุดในการให้บริการ ระบบได้ถูกออกแบบมารองรับสถานการณ์ฉุกเฉินดังนี้:

### 4.1. ความปลอดภัยของ API Key (Security)
- **Server-side Execution:** การเรียกใช้งาน AI เกิดขึ้นที่ฝั่ง Backend (`src/app/api/chat/route.ts`) ทั้งหมด หมายความว่า API Keys จะไม่ถูกเปิดเผยให้ผู้ใช้งานทั่วไปเห็นผ่านหน้าเว็บ (Client-side) เด็ดขาด ป้องกันการถูกขโมยคีย์ไปใช้งาน
- **Environment Variables:** แนะนำให้ย้าย API Keys ที่ฝังไว้ในโค้ด ไปตั้งค่าผ่านหน้าเว็บ Vercel (ในเมนู Settings > Environment Variables) และเรียกใช้ผ่าน `process.env` เพื่อไม่ให้รหัสผ่านถูกบันทึกลงในฐานข้อมูล Source Code (GitHub)

### 4.2. การรับมือกรณี API ติดลิมิต (Rate Limit Fallback & Retry Logic)
ในกรณีที่มีผู้ใช้งานจำนวนมากพร้อมกันจน AI API บางตัวถูกระงับชั่วคราว (Rate Limit / 429 Too Many Requests) ระบบจะมีการจัดการอัตโนมัติดังนี้:
1. **API Key Rotation & Retry Loop:** ทันทีที่ระบบพบว่า Key ตัวที่ 1 ติดลิมิต ระบบจะสลับไปใช้ Key ตัวที่ 2, 3, ... (ที่เตรียมไว้) ทันทีแบบอัตโนมัติ (Retry Loop) โดยที่ผู้ใช้จะไม่ทันสังเกตเห็นข้อผิดพลาด
2. **Graceful Error UI:** หากโชคร้ายที่ API Keys "ทุกตัว" ติดลิมิตพร้อมกันจนหมด ระบบจะไม่ยอมให้แอปพลิเคชันค้าง หรือพัง (Crash) แต่จะส่งสัญญาณแจ้งเตือนกลับมาที่หน้าเว็บ และแสดงข้อความที่สวยงามและสุภาพว่า *"⚠️ ขณะนี้มีผู้ใช้งานจำนวนมาก (API Limit Reached) กรุณาลองใหม่อีกครั้งในอีกสักครู่ครับ 🙏"* เพื่อให้ผู้ใช้เข้าใจสถานการณ์และรอใช้งานในภายหลัง

### 4.3. ระบบเก็บ Log การใช้งาน Token (Token Usage Monitoring)
เพื่อให้นักพัฒนาสามารถติดตามปริมาณการใช้งาน Token และป้องกันปัญหาการใช้งานเกินขีดจำกัด (Quota Exceeded) ของ API ได้ ระบบได้มีการเขียนโค้ดดักจับ `onFinish` ไว้ที่ฟังก์ชันสตรีมมิ่งของ AI
- **การทำงาน:** ทุกครั้งที่บอทตอบคำถามเสร็จสมบูรณ์ ระบบจะส่งข้อมูลสรุป Token (Prompt Tokens, Completion Tokens, Total Tokens) และ API Key ที่ใช้ ไปเก็บไว้ที่ระบบ Log หลังบ้าน (Backend Console)
- **วิธีการดู Log:** เนื่องจากระบบเราทำงานแบบ Serverless บน Vercel ผู้ดูแลระบบสามารถเข้าไปดู Token Log แบบ Real-time ได้ฟรี โดยเข้าไปที่ **Vercel Dashboard > เลือกโปรเจกต์ > แถบ Logs** แล้วค้นหาคำว่า `[TOKEN LOG]` ระบบจะแสดงประวัติการใช้งาน Token ของแต่ละคำถามอย่างละเอียดครับ
